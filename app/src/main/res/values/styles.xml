<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="selectedWeek" parent="Theme.AppCompat.Light">
        <item name="android:unfocusedMonthDateColor">@color/lightestBlue</item>
        <item name="color">@color/lightRosePink</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="calendarDays" parent="Theme.AppCompat.Light.Dialog">
        <item name="colorBackgroundFloating">#FF0000</item>
        <item name="color">#0000FF</item>
        <item name="android:textStyle">bold</item>
    </style>

</resources>
